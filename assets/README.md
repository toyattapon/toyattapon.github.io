# assets/

เก็บไฟล์พวกนี้ไว้ที่นี่:

- รูปโปรไฟล์ / screenshot โปรเจกต์ (เช่น `home-service-screenshot.png`)

**รูปโปรไฟล์:** `profile.jpg` (375×500px, ~32KB) คือไฟล์ที่เว็บใช้จริง — ถ้าเปลี่ยนรูปใหม่ ควรย่อ/บีบอัดให้ขนาดไฟล์เล็กก่อน (ใช้ `sips` บน Mac หรือเว็บ squoosh.app ก็ได้) แล้วเซฟทับชื่อ `profile.jpg` เดิม

หมายเหตุ: ไม่ต้องใส่ resume.pdf แล้ว — ปุ่มใน header ลิงก์ไป LinkedIn โดยตรงแทน
(resume ที่เป็นไฟล์จริงส่งให้ recruiter ตรงๆ ทาง email/LinkedIn ตอนสมัครงานแทน ไม่ต้อง publish เป็น public link)

แล้วอัปเดต path ใน `index.html` ให้ตรงกับชื่อไฟล์จริง
