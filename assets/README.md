# assets/

เก็บไฟล์พวกนี้ไว้ที่นี่:

- `resume.pdf` — เรซูเม่ของทอย (ลิงก์ปุ่ม "Resume" ใน header ชี้มาที่นี่)
- รูปโปรไฟล์ / screenshot โปรเจกต์ (เช่น `home-service-screenshot.png`)

แล้วอัปเดต path ใน `index.html` ให้ตรงกับชื่อไฟล์จริง
